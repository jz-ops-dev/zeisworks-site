---
// Permanent redirect handled by Netlify ([[redirects]] in netlify.toml).
// This file kept so the Astro build doesn't 404 on /stack during local dev.
return Astro.redirect('/what-i-use', 301);
---
