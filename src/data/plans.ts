// Add one entry per client: slug → full Growbotik URL.
// When Growbotik supports iframe embedding, ask them to whitelist
// zeisworks.com so the plan can load inline instead of opening a new tab.
export const plans: Record<string, string> = {
  "demo": "https://app.growbotik.com/ab3e6ce00d/plan/marketing_plans/3ef38b1191",
};
